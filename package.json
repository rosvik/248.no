{
  "name": "commit-hash-test",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "COMMIT_HASH=\"$(git rev-parse --short HEAD)\" astro dev --hash=test",
    "start": "COMMIT_HASH=\"$(git rev-parse --short HEAD)\" astro dev --hash=test",
    "build": "COMMIT_HASH=\"$(git rev-parse --short HEAD)\" astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.1"
  }
}
